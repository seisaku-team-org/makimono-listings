# 巻物を出品する

Markdown を1ファイル追加する PR を送るだけです。**アカウント登録も審査待ちもありません。**

## 手順

1. このリポジトリを fork する
2. `listings/<slug>.md` を1ファイル追加する (`<slug>` は英小文字・数字・ハイフン)
3. PR を出す — CI が形式と秘密情報を自動チェックします
4. マージされると [マキモノ](https://makimono-md.vercel.app) に掲載されます

手元で先に確認する場合:

```
node scripts/validate.mjs listings/your-slug.md
```

## ファイルの形

```markdown
---
slug: your-slug
title: "何をAIにやらせるMDか、ひと目で分かる題名"
summary: "1〜2文の説明。検索結果とAIの判断材料になります。"
category: "業務自動化"
tags: ["Gmail", "自動化"]
author: "あなたの名前 or ハンドル"
authorType: "community"
price: 0
version: "1.0.0"
createdAt: "2026-08-27"
updatedAt: "2026-08-27"
scratchTokens: 400000
withMdTokens: 50000
aiCompat: ["claude-code", "cursor"]
license: "CC BY 4.0"
---

# 見出し

## 作るもの

(AI がこれを読んだだけで実装に入れる粒度で書いてください)
```

### 項目の意味

| 項目 | 説明 |
| --- | --- |
| `scratchTokens` | この指示書なしでゼロから作らせた場合の推定消費トークン |
| `withMdTokens` | この指示書を読ませて作らせた場合の推定消費トークン |
| `category` | 業務自動化 / 開発プロセス / Google Workspace / Web開発 / データ収集 / SNS運用 / 経理・バックオフィス / AIのしつけ から選ぶ (新設も可) |
| `price` | いまは全件無料です。`0` を入れてください |

`scratchTokens` と `withMdTokens` は概算で構いませんが、**実際に測った値**を入れてください。
盛った数字は利用者にすぐ見抜かれ、マーケット全体の信頼を落とします。

## 中身の書き方

良い巻物は「調べ直さなくて済む」ものです。次が入っていると価値が出ます。

- **判断の順序** — 何を先に決めるか
- **踏んだ罠** — なぜその方法ではダメだったか
- **検証方法** — 動いたとどう確認するか
- **完成条件** — どうなったら終わりか

逆に、公式ドキュメントを写しただけのものは価値が出ません。

## 出してはいけないもの

- APIキー・トークン・パスワード・Webhook URL などの秘密値 (CI が検出して落とします)
- 顧客名・社内固有の識別子・個人情報
- 他人の著作物をそのまま貼ったもの

## ライセンス

出品した内容は、あなたが指定した `license` の条件で公開されます。
指定がなければ `CC BY 4.0` として扱います。
